/*
 * Public landing — Vite entry point
 *
 * Mounted at apex `/` per multi-entry config in vite.config.js (Day 2).
 * Loads tokens via index.html <link>, then defers to landing.js for
 * orchestration.
 */
import './landing.js'
